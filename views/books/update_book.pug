extends ../layout 

block content 
    h1= title 

    include errors
  
    form(action="/books/" + book.id, method="post")
        include form 
        br
        a.button(href=`/books/${book.id}/delete`) Delete Book